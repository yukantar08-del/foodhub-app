name: foodhub

description: FoodHub v0.6 prototype - customer, merchant, driver, and admin roles.
publish_to: "none"
version: 0.6.0+6

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
